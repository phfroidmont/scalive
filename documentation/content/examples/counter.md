{%
title = "Typed counter"
description = "A LiveView with a count model and Decrement, Reset, and Increment messages."
order = 1
section = examples
%}

A LiveView with a `count` model and `Decrement`, `Reset`, and `Increment`
messages.

@:example(counter)

Related guidance: [start with models, typed messages, and rendering](../learn/index.md#start-here).
